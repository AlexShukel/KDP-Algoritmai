node_modules
dist
dataset-generator
data
problems
seed-dataset.csv
temp-results
*.tex
*.txt

# Rust workspace build artifacts
target/

# Python virtualenv for vrppd-or-tools (created via
# `python3 -m venv .venv` per BENCHMARKS.md "OR-Tools baseline setup").
.venv/

# Third-party copyrighted papers used as references but not redistributable.
# The author's own thesis PDFs (Kursinis_darbas.pdf, Kursinio_darbo_projektas.pdf)
# are intentionally NOT excluded — those belong to the project author.
documents/WC13_*.pdf
.DS_Store
results/benchmark-results-*.json

# Python bytecode cache (vrppd-or-tools/python).
__pycache__/

# Claude Code harness runtime state.
.claude/scheduled_tasks.lock

# Ad-hoc archive of pre-2026-05-12-sweep results + problems. Kept on disk as a
# local safety net; never tracked.
.archive-2026-05-12/

# Temporary problems-dir stash used by scripts/run-r05-ortools-routing-pci.sh
# while the script swaps classes in and out. Auto-cleaned on script exit.
.ortools-pci-stash/
